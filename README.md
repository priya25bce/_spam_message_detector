# _spam_message_detector
Spam Message Detector is a project that detects whether the message is spam or not. The programming language used here is Python with machine learning (TF-IDF and Naive Bayes Algorithm) with both CLI and GUI interface.
